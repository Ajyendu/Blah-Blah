# Frontend env — copy to .env and fill in.
# Local dev: use VITE_BACKEND_URL below. Production (Vercel): set VITE_BACKEND_URL in the project dashboard (no .env file there).

# Backend API + Socket URL (no trailing slash)
# Local: backend running on 5050. Production: your Render backend URL.
VITE_BACKEND_URL=http://localhost:5050

# Optional: image uploads (if frontend uses Cloudinary directly)
# CLOUDINARY_CLOUD_NAME=...
# CLOUDINARY_API_KEY=...
# CLOUDINARY_API_SECRET=...
